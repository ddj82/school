CREATE TABLE NAVERMEM(
ID VARCHAR2(20),
PASSWORD VARCHAR2(16) NOT NULL,
MAIL VARCHAR2(100),
NAME VARCHAR2(12) NOT NULL,
BIRTH DATE NOT NULL,
GENDER VARCHAR2(1) CHECK (GENDER IN('M','F','N')) NOT NULL,
PHONE VARCHAR2(20) NOT NULL,
PRIMARY KEY(ID)
);